#include <stdio.h>

/**
* main - display all single digits
*
* Description: using the main function, this will
* display all single digits in base 10.
* Return: 0
*
*/
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	return (0);
}
